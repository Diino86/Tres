cards = []
colors = ['r', 'b', 'g', 'y']
special = ['sk', 'rv', 'dr2']
unique = ['w', 'wdr4']

class Cards:
    def __init__(self, m, c, n):
        self.multiply = m
        self.color = c
        self.number = n
    
    def multiply2(self):
        for _ in range(2):
            self.multiply
            
    def color(self):
        for color in colors:
            self.color
            
    def cardNumber(self):
        for numb in range (1, 10):
            self.number
        
normalCards = Cards(color(), cardNumber(), cards.append(number, color))